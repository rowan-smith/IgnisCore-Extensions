id: sprinkler-timer

display:
  title: "&9Sprinkler Timer"
  description:
    - "&7Right-click a &esprinkler head&7 to link."
    - "&7Use again to control it remotely."

item:
  base_material: paper

textures:
  icon: icon.png
  fallback: remote

behavior:
  right_click_air: use
  right_click_block: use

custom_data:
  linkBlockType: "sprinkler-head"
  remoteAction: "arm"
  linkRange: 48
