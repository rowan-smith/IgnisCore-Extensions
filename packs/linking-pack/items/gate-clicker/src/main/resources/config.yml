id: gate-clicker

display:
  title: "&9Gate Clicker"
  description:
    - "&7Right-click a &ekeyed hatch&7 to link."
    - "&7Use again to control it remotely."

item:
  base_material: paper

textures:
  icon: icon.png
  fallback: remote

behavior:
  right_click_air: use
  right_click_block: use

custom_data:
  linkBlockType: "keyed-hatch"
  remoteAction: "toggle"
  linkRange: 64
