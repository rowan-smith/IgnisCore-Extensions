id: rivet-gun

display:
  title: "&6Rivet Gun"
  description:
    - "&7Reinforce a block into &8obsidian&7 (or configured material)."
    - "&7Cannot rivet already-reinforced blocks."
    - "&7Consumes one rivet per use."

item:
  base_material: paper

textures:
  icon: icon.png
  fallback: tool

behavior:
  right_click_air: use
  right_click_block: use

custom_data:
  reinforcedMaterial: "obsidian"
