id: cable-ties

display:
  title: "&6Cable Ties"
  description:
    - "&7Click two &ffence posts&7 to tie them."
    - "&7Draws a particle cable between posts."
    - "&7Consumes one tie per &elink&7."

item:
  base_material: paper

textures:
  icon: icon.png
  fallback: tool

behavior:
  right_click_air: use
  right_click_block: use
