id: keyring-beacon

display:
  title: "&bKeyring Beacon"
  description:
    - "&7Save up to &f3 beacon slots&7 with your coords."
    - "&7Cycles through numbered slots on each use."
    - "&7Pulses an end-rod ring when marking."

item:
  base_material: paper

textures:
  icon: icon.png

behavior:
  right_click_air: use
  right_click_block: use

custom_data:
  beaconSlots: 3
