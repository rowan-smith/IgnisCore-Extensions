id: keyring-beacon

display:
  title: "&b&lKeyring Beacon"
  description:
    - "&r&r&7Save up to &f3 beacon slots&7 with your coords."
    - "&r&r&7Cycles through numbered slots on each use."
    - "&r&r&7Pulses an &bend-rod&7 &ering&7 when marking."

item:
  base_material: paper

textures:
  icon: icon.png
  fallback: map

behavior:
  right_click_air: use
  right_click_block: use

custom_data:
  beaconSlots: 3
