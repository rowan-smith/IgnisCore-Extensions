id: chunk-grid-overlay

display:
  title: "&bChunk Grid Overlay"
  description:
    - "&7Use to show your current &fchunk coords&7."
    - "&7Displays chunk X/Z on the action bar."
    - "&7Pulses a flame &ering&7 at the chunk corner."

item:
  base_material: paper

textures:
  icon: icon.png
  fallback: map

behavior:
  right_click_air: use
  right_click_block: use
